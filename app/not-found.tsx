import React from "react";
import NotFound from "@/components/NotFound/NotFound";

// THis is the component that will be render for all the route that could not be found.
const NotFoundPage = () => {
  return <NotFound />;
};

export default NotFoundPage;
